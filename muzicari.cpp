#include <iostream>
#include <vector>

using namespace std;

int main() {
  int T, N;
  cin >> T >> N;

  vector<int> v(N);

  for (auto &e : v) {
    cin >> e;
  }

  return 0;
}

int solve(vector<int> &v, int T, int n) {}
